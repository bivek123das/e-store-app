//** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com',
      'fakestoreapi.com',
      'i.ibb.co',
      'your-cdn.com',
    ],
  },
};

export default nextConfig;
